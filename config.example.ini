[anthropic]
; Copy this file to "config.ini" (same folder as the .exe / main.py) and paste
; your Anthropic API key below. config.ini is gitignored so your key stays local.
api_key = sk-ant-REPLACE-WITH-YOUR-KEY
